# Backend API base URL (Render URL in production)
VITE_API_URL=http://localhost:8000

# Clerk publishable key — same value as CLERK_PUBLISHABLE_KEY in backend/.env.example
# In Vercel, set this in Project Settings > Environment Variables (not committed here)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxx
